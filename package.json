{
  "name": "cra-generate",
  "version": "3.0.2",
  "description": "Scaffold react components",
  "main": "index.js",
  "repository": "rthor/cra-generate",
  "author": "rthor",
  "license": "MIT",
  "bin": {
    "cra-generate": "./index.js"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "change-case": "^3.0.0",
    "commander": "^2.9.0",
    "mkpath": "^1.0.0"
  },
  "lint-staged": {
    "*.js": [
      "prettier",
      "git add"
    ]
  },
  "scripts": {
    "prettier": "prettier --single-quote --no-semi --trailing-comma es5 --write \"{{templates,lib}/**/*.js,*.js}\"",
    "precommit": "lint-staged",
    "test": "jest"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/coverage/",
      "/src/",
      "/templates/"
    ]
  },
  "devDependencies": {
    "husky": "^0.13.3",
    "jest": "^19.0.2",
    "lint-staged": "^3.4.0",
    "prettier": "1"
  }
}
