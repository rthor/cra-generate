{
  "name": "cra-generate",
  "version": "3.0.2",
  "description": "Scaffold react components",
  "main": "index.js",
  "repository": "rthor/cra-generate",
  "author": "rthor",
  "license": "MIT",
  "bin": {
    "cra-generate": "./index.js"
  },
  "engines": {
    "node": ">=6"
  },
  "dependencies": {
    "chalk": "^1.1.3",
    "change-case": "^3.0.0",
    "commander": "^2.9.0",
    "mkpath": "^1.0.0"
  },
  "lint-staged": {
    "*.ts": ["prettier", "git add"]
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "prepublish": "yarn run build",
    "prettier": "prettier --parser \"typescript\" --no-semi --trailing-comma es5 --write \"{{templates,src}/**/*.{js,ts},*.{js,ts}}\"",
    "precommit": "lint-staged",
    "test": "yarn run build && jest"
  },
  "jest": {
    "rootDir": "lib"
  },
  "devDependencies": {
    "@types/chalk": "^0.4.31",
    "@types/jest": "^20.0.2",
    "@types/mkpath": "^0.1.29",
    "@types/node": "^8.0.13",
    "husky": "^0.13.3",
    "jest": "^20.0.4",
    "lint-staged": "^3.4.0",
    "prettier": "^1.5.3",
    "typescript": "^2.4.1"
  }
}
